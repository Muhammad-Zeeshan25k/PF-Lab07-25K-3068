#include <stdio.h>
int main(){
	int record[10],b;
	for (int i=0;i<10;i++)
	{
		printf("Enter number in %d index :",i);
		scanf("%d",&b);
		record[i]=b;
	}
	for (int j=0;j<10;j++){
		for (int k=0;k<10;k++)
		{
		 if (record[j]==record[k]&&j!=k)
		 {record[k]=-1;
		 }
		}}
			for (int i=0;i<10;i++)
	{
		printf("Enter number in index :%d\n",record[i]);
		
	}
	return 0;
}
