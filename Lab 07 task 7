#include <stdio.h>
int main(){
    int record[10], b, repeat[100] = {0};
    for (int i = 0; i < 10; i++){
        printf("Enter number in %d index :", i);
        scanf("%d", &b);
        record[i] = b;
    }
    for (int j = 0; j < 10; j++){
        if (repeat[record[j]] == 0){
            repeat[record[j]] = 1;
        }
        else{
            record[j] = -1;
        }}
    for (int i = 0; i < 10; i++){
        printf("Enter number in index :%d\n", record[i]);
    }
    return 0;
}
